@{ 

}

@using System.Data;

<!DOCTYPE html>
<html lang="en">

<head>
    <style>
        * {
            font-family: 'Times New Roman';
        }

        h4 {
            text-align: center;
            color: blue;
        }

        .add-new-button {
            background-color: #28a745; /* Green color */
            color: #fff; /* White text color */
            border: none;
            border-radius: 5px; /* Rounded corners */
            padding: 10px 20px; /* Adjust padding as needed */
            font-size: 16px; /* Adjust font size as needed */
            /*/float: right; /* Adjust margin to position it to the right */
            margin-left: auto; /* Auto margin left to push it to the right */
            display: flex; /* Use flexbox to control layout */
        }

        td button {
            background-color: transparent;
            border: 2px solid #007bff;
            color: #007bff;
            /* padding: 10px 20px;*/
            font-size: 13px;
            cursor: pointer;
            transition: all 0.3s ease;
            border-radius: 5px;
        }

            td button:hover {
                background-color: #007bff;
                color: #fff;
            }

            /* Optional: Adjust the button size */
            td button.large {
                padding: 15px 30px;
                font-size: 18px;
            }

            /* Optional: Adjust the button color */
            td button.primary {
                background-color: #007bff;
                border-color: #007bff;
                color: #fff;
            }

                td button.primary:hover {
                    background-color: #0056b3;
                    border-color: #0056b3;
                }

        .print {
            background-color: transparent;
            border: 2px solid orange;
            color: orange;
            padding: 10px 20px; /* Adjust padding for a balanced look */
            font-size: 14px; /* Slightly larger font for better readability */
            text-decoration: none; /* Remove underline */
            cursor: pointer;
            transition: all 0.3s ease;
            border-radius: 5px;
            display: inline-block; /* Ensure proper padding and margin */
            font-family: 'Times New Roman', sans-serif; /* Optional: Choose a clean font */
        }

            /* Hover effect */
            .print:hover {
                background-color: orange;
                color: #fff;
                box-shadow: 0 0 10px rgba(255, 165, 0, 0.2); /* Optional: Add a shadow effect */
            }

            /* Optional: Focus effect for accessibility */
            .print:focus {
                outline: none;
                box-shadow: 0 0 5px rgba(255, 165, 0, 0.5);
            }

        .editBtn {
            background-color: transparent;
            border: 2px solid #47b447;
            color: #47b447;
            padding: 10px; /* Adjust padding for a balanced look */
            font-size: 14px; /* Slightly larger font for better readability */
            text-decoration: none; /* Remove underline */
            cursor: pointer;
            transition: all 0.3s ease;
            border-radius: 5px;
            display: inline-block; /* Ensure proper padding and margin */
            font-family: 'Times New Roman', sans-serif; /* Optional: Choose a clean font */
        }

            /* Hover effect */
            .editBtn:hover {
                background-color: #47b447;
                color: #fff;
                box-shadow: 0 0 10px rgba(0, 0, 255, 0.2); /* Optional: Add a shadow effect */
            }

            /* Optional: Focus effect for accessibility */
            .editBtn:focus {
                outline: none;
                box-shadow: 0 0 5px rgba(0, 0, 255, 0.5);
            }

        .delete-button {
            background-color: transparent;
            border: 2px solid red;
            color: red;
            padding: 10px 20px; /* Adjust padding for a balanced look */
            font-size: 14px; /* Slightly larger font for better readability */
            cursor: pointer;
            transition: all 0.3s ease;
            border-radius: 5px;
            font-family: 'Times New Roman', sans-serif; /* Optional: Choose a clean font */
        }

            /* Hover effect */
            .delete-button:hover {
                background-color: red;
                color: #fff;
                box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Optional: Add a shadow effect */
            }

            /* Optional: Large button size */
            .delete-button.large {
                padding: 15px 30px;
                font-size: 18px;
            }

            /* Optional: Primary button style */
            .delete-button.primary {
                background-color: red;
                border-color: red;
                color: #fff;
            }

                .delete-button.primary:hover {
                    background-color: darkred;
                    border-color: darkred;
                }

            /* Optional: Add focus effect for accessibility */
            .delete-button:focus {
                outline: none;
                box-shadow: 0 0 5px rgba(255, 0, 0, 0.5);
            }
    </style>
    <meta charset="utf-8">
    <meta content="width=device-width, initial-scale=1.0" name="viewport">
    <title>Danh Sách Bảng Kê Nộp Tiền</title>
    <meta content="" name="description">
    <meta content="Author" name="MJ Maraz">
    <link href="~/assets/images/logo.png" rel="icon">
    <link href="~/assets/images/favicon.png" rel="apple-touch-icon">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
    <!-- ========================================================= -->


    <link rel="stylesheet" href="~/assets/css/bootstrap.min.css">
    <link rel="stylesheet" href="~/assets/css/datatables.min.css">






</head>
<!-- =============== Design & Develop By = MJ MARAZ   ====================== -->

<body style="background-color: #e0e8ee ">
    @*<div class="tab-control" style="position:fixed;bottom:50px">
            <ul class="tabs" style="background-color: #1d1b31">
                <li class="tab selected"><a href="/Home/About" style="padding-right:5px">Home</a></li>

            </ul>
        </div>*@


    <header class="header_part" style="background-color: #aec9e0; color: #3483bd ">
        @if (Request.Cookies["UserName"].Value == "admin-cnct" || Request.Cookies["UserName"].Value == "admin-cntg" || Request.Cookies["UserName"].Value == "admin-cnmd" || Request.Cookies["UserName"].Value == "admin-cnh1" || Request.Cookies["UserName"].Value == "admin-cnvt"
             || Request.Cookies["UserName"].Value == "admin-cnnt" || Request.Cookies["UserName"].Value == "admin - cndn" || Request.Cookies["UserName"].Value == "admin - cnna" || Request.Cookies["UserName"].Value == "admin - cnhn")
        {



            <a href="/Home/About1" style="cursor:pointer">
                <img src="~/Images/opc.png" style="height:60px;width:60px;margin-left:10px">
            </a>
        }
        else
        {
            <a href="/Home/About" style="cursor:pointer">
                <img src="~/Images/opc.png" style="height:60px;width:60px;margin-left:10px">
            </a>
        }
        <h4 style="color:#3483bd">Danh Sách Bảng Kê Nộp Tiền Ngân Hàng</h4>
    </header>
    <!-- =======  Data-Table  = Start  ========================== -->
    <div class="container">
        <div class="row">
            <div class="col-12">
                <button type="button" id="addNewBtn" class="add-new-button">Thêm mới</button>

                <div class="data_table">
                    <table id="example" class="table table-striped table-bordered">
                        <thead class="table-dark">
                            <tr>
                                <th>Ngày Chứng từ</th>
                                <th>Số Chứng Từ</th>
                                <th>Mã TDV</th>
                                <th>Tên TDV</th>
                                <th>Người Nộp Tiền</th>
                                <th>Nội dung</th>
                                <th>Mã Đvcs</th>
                                <th>Tổng tiền</th>
                                <th></th>
                                @*<th></th>*@
                                <th hidden>STT</th>
                                <th></th>
                                <th></th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (DataRow row2 in Model.Tables[0].Rows)
                            {
                                string Ma_CbNv = Request.Cookies["Ma_CbNv"] != null ? Request.Cookies["Ma_CbNv"].Value : "";
                                if (Request.Cookies["UserName"] != null && Request.Cookies["UserName"].Value.StartsWith("OPC", StringComparison.OrdinalIgnoreCase))
                                {
                                    Ma_CbNv = "";
                                }
                                if (Ma_CbNv != "" && Ma_CbNv != "100000")
                                {
                                    if (Request.Cookies["Ma_CbNv"].Value == row2["Ma_TDV"].ToString())
                                    {

                                        var currentSoCt = row2["Stt"].ToString();
                                        <tr data-soct="@currentSoCt">
                                            <td style="text-align:right" class="Ngay_Ct">@row2["Ngay_Ct"]</td>
                                            <td class="so-bk">@row2["So_BK"]</td>
                                            <td class="ma-tdv">@row2["Ma_TDV"]</td>
                                            <td class="ten-tdv">@row2["Ten_TDV"]</td>
                                            <td class="nguoi-nop-tien">@row2["Nguoi_Nop_Tien"]</td>
                                            <td class="noi-dung">@row2["Noi_Dung"]</td>
                                            <td>@row2["Ma_dvcs"]</td>
                                            <td style="text-align:right">@string.Format("{0:n0}", row2["Tong_Tien"]).Replace(",", ".")</td>
                                            <td><a href="#" class="editBtn">EDIT</a></td>
                                            <td>
                                                <form action="javascript:void(0);" method="post" onsubmit="deleteRow(this, '@row2["Stt"]');">
                                                    <input type="hidden" name="stt" value="@row2["STT"]" />
                                                    <button type="submit" class="delete-button">DELETE</button>
                                                </form>
                                            </td>
                                            <td><a href="#" class="print" onclick="printRow(this)">PRINT</a></td>

                                            <td hidden class="ctvid">@row2["Stt"]</td>
                                            <td><button onclick="scrollToBottom()">SEE DETAIL</button></td>
                                        </tr>
                                    }
                                }
                                else if (Ma_CbNv == "100000")
                                {

                                    var currentSoCt = row2["Stt"].ToString();
                                    <tr data-soct="@currentSoCt">
                                        <td style="text-align:right" class="Ngay_Ct">@row2["Ngay_Ct"]</td>
                                        <td class="so-bk">@row2["So_BK"]</td>
                                        <td class="ma-tdv">@row2["Ma_TDV"]</td>
                                        <td class="ten-tdv">@row2["Ten_TDV"]</td>
                                        <td class="nguoi-nop-tien">@row2["Nguoi_Nop_Tien"]</td>
                                        <td class="noi-dung">@row2["Noi_Dung"]</td>
                                        <td>@row2["Ma_dvcs"]</td>
                                        <td style="text-align:right">@string.Format("{0:n0}", row2["Tong_Tien"]).Replace(",", ".")</td>
                                        <td><a href="#" class="editBtn">EDIT</a></td>
                                        <td>
                                            <form action="javascript:void(0);" method="post" onsubmit="deleteRow(this, '@row2["Stt"]');">
                                                <input type="hidden" name="stt" value="@row2["STT"]" />
                                                <button type="submit" class="delete-button">DELETE</button>
                                            </form>
                                        </td>
                                        <td><a href="#" class="print" onclick="printRow(this)">PRINT</a></td>

                                        <td hidden class="ctvid">@row2["Stt"]</td>
                                        <td><button onclick="scrollToBottom()">SEE DETAIL</button></td>
                                    </tr>

                                }
                                else
                                {
                                    var currentSoCt = row2["Stt"].ToString();
                                    <tr data-soct="@currentSoCt">
                                        <td style="text-align:right" class="Ngay_Ct">@row2["Ngay_Ct"]</td>
                                        <td class="so-bk">@row2["So_BK"]</td>
                                        <td class="ma-tdv">@row2["Ma_TDV"]</td>
                                        <td class="ten-tdv">@row2["Ten_TDV"]</td>
                                        <td class="nguoi-nop-tien">@row2["Nguoi_Nop_Tien"]</td>
                                        <td class="noi-dung">@row2["Noi_Dung"]</td>
                                        <td>@row2["Ma_dvcs"]</td>
                                        <td style="text-align:right">@string.Format("{0:n0}", row2["Tong_Tien"]).Replace(",", ".")</td>
                                        <td><a href="#" class="editBtn">EDIT</a></td>
                                        <td>
                                            <form action="javascript:void(0);" method="post" onsubmit="deleteRow(this, '@row2["Stt"]');">
                                                <input type="hidden" name="stt" value="@row2["STT"]" />
                                                <button type="submit" class="delete-button">DELETE</button>
                                            </form>
                                        </td>
                                        <td><a href="#" class="print" onclick="printRow(this)">PRINT</a></td>

                                        <td hidden class="ctvid">@row2["Stt"]</td>
                                        <td><button onclick="scrollToBottom()">SEE DETAIL</button></td>
                                    </tr>
                                }

                            }
                        </tbody>
                    </table>

                </div>
                <br />
                <br />
                <div class="data_table" style="text-align:center">
                    <h5 style="font-weight:bold">Chi Tiết Bảng Kê Nộp Tiền Ngân Hàng</h5>
                    <table id="example2" class="table table-striped table-bordered">
                        <thead class="table-dark">
                            <tr>
                                <th hidden></th>
                                <th>Số HD</th>
                                <th>Số CT</th>
                                <th>Ngày HD</th>
                                <th>Ngày Đến Hạn</th>
                                <th>Biên Lai</th>
                                <th>Mã DT</th>
                                <th>Tên DT</th>
                                <th>Tiền Trước Thuế</th>
                                <th>Tiền HD</th>
                                <th>Thuế</th>
                                <th>Tiền CKTT</th>
                                <th>Tiền Phải Thu</th>
                                <th>Tiền Còn Lại</th>
                                @*<th>Tiền Còn Lại</th>*@
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (DataRow row2 in Model.Tables[1].Rows)
                            {
                                <tr>
                                    <td hidden>@row2["Stt"]</td>
                                    <td style="text-align:right" class="NV_GN">@row2["So_Hd"]</td>
                                    <td style="text-align:left">@row2["So_CT"]</td>
                                    <td style="text-align: center">@row2["Ngay_HD"]</td>
                                    <td style="text-align:center">@row2["Ngay_Den_Han"]</td>
                                    <td style="text-align:center">@row2["Bien_Lai"]</td>
                                    <td style="text-align: center">@row2["Ma_Dt"]</td>
                                    <td style="text-align:left">@row2["Ten_Dt"]</td>
                                    <td style="text-align: right">@string.Format("{0:n0}", row2["Tien_Truoc_Thue"]).Replace(",", ".")</td>
                                    <td style="text-align: right">@string.Format("{0:n0}", row2["Tien_HD"]).Replace(",", ".")</td>
                                    <td style="text-align: right">@string.Format("{0:n0}", row2["Thue"]).Replace(",", ".")</td>
                                    <td style="text-align: right">@string.Format("{0:n0}", row2["Tien_CKTT"]).Replace(",", ".")</td>
                                    <td style="text-align:right">@string.Format("{0:n0}", row2["Tien_Phai_Thu"]).Replace(",", ".")</td>
                                    <td style="text-align:right">@string.Format("{0:n0}", row2["Tien_Con_Lai"]).Replace(",", ".")</td>
                                    @*<td style="text-align:right">@string.Format("{0:n0}", row2["Tien_Con_Lai"]).Replace(",", ".")</td>*@
                                </tr>
                            }
                        </tbody>
                    </table>

                </div>
            </div>
        </div>
    </div>
    <!-- =======  Data-Table  = End  ===================== -->
    <!-- ============ Java Script Files  ================== -->

    <div id="footer"></div>
    <script src="~/assets/js/bootstrap.bundle.min.js"></script>
    <script src="~/assets/js/jquery-3.6.0.min.js"></script>
    <script src="~/assets/js/datatables.min.js"></script>

    <script>
        $(document).ready(function () {
            $('#example').DataTable({
                "order": [[0, "desc"]], // Sắp xếp theo cột đầu tiên (Ngày Chứng từ) giảm dần
                "columnDefs": [
                    {
                        "targets": 0,  // Chỉ định cột Ngày Chứng từ
                        "render": function (data, type, row) {
                            if (type === 'sort' || type === 'type') {
                                var parts = data.split('/');
                                return new Date(parts[2], parts[1] - 1, parts[0]);
                            }
                            return data;
                        }
                    }
                ]
            });
        });
    </script>

    <script>
        function printRow(element) {
            // Lấy hàng hiện tại
            var row = element.closest('tr');

            // Lấy giá trị So_BK từ hàng
            var soBK = row.querySelector('.so-bk').textContent;

            // Chuyển đến một view mới ở tab mới
            var url = '/FormBKNopTienNH/MauInBangKe?soBK=' + soBK;
            window.open(url, '_blank');
        }
    </script>
    <script>
        function scrollToBottom() {
            var footer = document.getElementById("footer");
            if (footer) {
                footer.scrollIntoView({ behavior: "smooth", block: "end", inline: "nearest" });
            }
        }
    </script>
    <script>
            $(document).ready(function () {
                $('#example').DataTable();
            });
        </script>
    <script>
        // Your existing JavaScript code

        $(document).ready(function () {
            // Existing document.ready code

            $('#addNewBtn').click(function () {

                sessionStorage.clear();
                // Redirect to the specified URL when the button is clicked
                var newWindow = window.open('', '_blank');

                //window.location.href = '/congtacvien/InputCTV';
                newWindow.location.href = '/FormBKNopTienNH/InsertBangKeNH';
            });
        });

        $('.editBtn').click(function () {
            var Stt = $(this).closest('tr').find('.ctvid').text();
            var NV_GN = $(this).closest('tr').find('.NV_GN').text();
            var MaTDV = $(this).closest('tr').find('.ma-tdv').text();
            var NgayCt = $(this).closest('tr').find('.Ngay_Ct').text();
            var SoCt = $(this).closest('tr').find('.so-bk').text();
            var NgNopTien = $(this).closest('tr').find('.nguoi-nop-tien').text();
            var NoiDung = $(this).closest('tr').find('.noi-dung').text();
            var TenTDV = $(this).closest('tr').find('.ten-tdv').text();
            var newWindow = window.open('', '_blank');
            document.cookie = "stt=" + Stt + ";path=/;expires=Fri, 31 Dec 9999 23:59:59 GMT";
            document.cookie = "NV_GiaoHang=" + NV_GN + ";path=/;expires=Fri, 31 Dec 9999 23:59:59 GMT";

            newWindow.location.href = '/FormBKNopTienNH/UpdateBangKeNH?Stt=' + Stt + '&Ngay_Ct=' + NgayCt + '&So_Ct=' + SoCt + '&Ma_TDV=' + MaTDV + '&Nguoi_Nop_Tien=' + NgNopTien + '&Ten_TDV=' + TenTDV + '&NoiDung=' + NoiDung;

        });


    </script>
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            console.log("DOM đã sẵn sàng");
            var table1Rows = document.querySelectorAll("#example tbody tr");
            var table2Rows = document.querySelectorAll("#example2 tbody tr");

            // Ẩn tất cả các hàng trong bảng thứ hai
            table2Rows.forEach(function (row2) {
                row2.style.display = "none";
            });

            // Gán sự kiện click cho các nút bấm trong bảng thứ nhất
            table1Rows.forEach(function (row) {
                var button = row.querySelector("td:last-child button");
                if (button) {
                    button.addEventListener("click", function () {
                        var soCt = row.getAttribute("data-soct").toLowerCase(); // chuyển về chữ thường
                        console.log("Sự kiện click đã được kích hoạt");
                        console.log("Số chứng từ:", soCt);

                        // Ẩn tất cả các hàng trong bảng thứ hai
                        table2Rows.forEach(function (row2) {
                            row2.style.display = "none";
                        });

                        // Hiện các hàng có giá trị tương ứng với số chứng từ
                        if (soCt !== "") {
                            console.log("Đã kiểm tra");
                            table2Rows.forEach(function (row2) {
                                var maDt = row2.querySelector("td:first-child").textContent.trim().toLowerCase(); // chuyển về chữ thường
                                console.log("So sánh:", maDt, soCt);
                                if (maDt === soCt) {
                                    console.log("Khớp:", maDt, soCt);
                                    row2.style.display = "table-row";
                                }
                            });
                        }
                    });
                    console.log("Sự kiện click đã được gán cho nút bấm");
                } else {
                    console.log("Không tìm thấy nút bấm trong hàng:", row);
                }
            });
        });


    </script>
    <script>

        // Hàm để thiết lập cookie
        function setCookie(name, value, days) {
            var expires = "";
            if (days) {
                var date = new Date();
                date.setTime(date.getTime() + (days * 24 * 60 * 60 * 1000));
                expires = "; expires=" + date.toUTCString();
            }
            document.cookie = name + "=" + (value || "") + expires + "; path=/";
        }

        // Thêm sự kiện click cho các nút "Chỉnh Sửa"
        document.querySelectorAll('.editBtn').forEach(function (editBtn) {
            editBtn.addEventListener('click', function (event) {
                event.preventDefault(); // Ngăn chặn hành động mặc định của thẻ <a>
                var row = this.closest('tr'); // Lấy dòng chứa nút được click
                var maTdv = row.querySelector('.ma-tdv').textContent; // Lấy giá trị Ma_TDV
                var stt = row.querySelector('.ctvid').textContent; // Lấy giá trị Ma_TDV
                console.log('Ma_TDV:', maTdv); // Kiểm tra giá trị Ma_TDV
                setCookie('Ma_CbNv', maTdv, 7); // Lưu giá trị vào cookie
                setCookie('Stt', stt, 7); // Lưu giá trị vào cookie

            });
        });

    </script>
    <script>
        window.onload = function () {
            sessionStorage.removeItem('tableRows');
            sessionStorage.removeItem('tienConLaiValues');
            sessionStorage.removeItem('tienTruocThueValues');
            sessionStorage.removeItem('tienThueValues');
            sessionStorage.removeItem('tienPhaiThuValues');

        };
    </script>
    <script>


        function deleteRow(form, stt) {
           if (confirm('Bạn có chắc chắn muốn xoá dòng này không?')) {
        $.ajax({
            url: '@Url.Action("Delete", "FormBKNopTienNH")',
            type: 'POST',
            data: { STT: stt },
            success: function (response) {
                if (response === 'Success! Đã Xoá') {
                    // Remove the row from the table
                    $(form).closest('tr').remove();
                    alert(response);
                    window.location.href = '/FormBKNopTienNH/Index';
                } else {
                    alert(response);
                }
            },
            error: function () {
                alert('Error occurred while deleting the record.');
            }
        });
    }
}



    </script>


</body>

</html>
