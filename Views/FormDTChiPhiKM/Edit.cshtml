@using System.Data;
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no">
    <title>Form cộng tác viên</title>
    <link rel="stylesheet" href="/assets/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="/assets/css/Application-Form.css">
    <style>
        * {
            font-family: serif;
        }

        #dropdownInput, #selectedOption {
            border: 1px solid #ccc;
            border-radius: 5px;
            padding: 5px;
            margin-bottom: 5px;
            display: inline-block;
        }

        #dropdownInput {
            width: 18%; /* Adjust width as needed */
        }

        #selectedOption {
            width: 80%; /* Adjust width as needed */
        }

        .ma-vt-input,
        .ten-vt-input,
        .ten-ctkm-input {
            border-radius: 5px;
            padding: 5px;
            margin-bottom: 5px;
            display: inline-block;
            width: 80px; /* Adjust width as needed */
        }

        .ma-ctkm-input {
            border-radius: 5px;
            padding: 5px;
            margin-bottom: 5px;
            display: inline-block;
            width: 80px; /* Adjust width as needed */
        }

        .ten-ctkm-input {
            width: 20%; /* Adjust width as needed */
        }

        .doanh-thu-input {
            border-radius: 5px;
            padding: 5px;
            margin-bottom: 5px;
            display: inline-block;
            width: 80px; /* Adjust width as needed */
        }

        .cpkm-input {
            border-radius: 5px;
            padding: 5px;
            margin-bottom: 5px;
            display: inline-block;
            width: 80px; /* Adjust width as needed */
        }

        .ty-le-input {
            border-radius: 5px;
            padding: 5px;
            margin-bottom: 5px;
            display: inline-block;
            width: 80px; /* Adjust width as needed */
        }

        #SaveCtV {
            display: block;
            margin: 10px auto; /* Center the buttons and add some spacing */
            padding: 10px 20px; /* Adjust padding as needed */
            font-size: 16px; /* Adjust font size as needed */
            background-color: #28a745; /* Green color */
            color: #fff; /* White text color */
            border: none;
            border-radius: 5px;
            cursor: pointer;
            background-color: #218838; /* Darker green on hover */
        }
    </style>
</head>

<body>
    <section>
        <h1 class="text-center text-capitalize">CHỈNH SỬA</h1>
        <div class="container">

            <form action="SaveCtV" method="post">
                @*<form>*@
                @foreach (DataRow row2 in Model.Tables[0].Rows)
                {
                    <div class="form-group mb-3">
                        <div class="row">
                            <div class="col">
                                <p><strong>1. Ngày</strong>&nbsp;<span class="text-danger">*</span></p><input class="form-control" type="date" required="" name="Ngay_Ct" id="Ngay_Ct"value="@row2["Ngay_Ct"]">
                            </div>
                            <div class="col">
                                <p><strong>2. Số Chứng Từ</strong>&nbsp;<span class="text-danger">*</span></p><input class="form-control" type="text" required="" name="So_Ct" id="So_Ct"value="@row2["So_Ct"]" >
                            </div>
                        </div>
                    </div>
                    break;
                }
            <table id="example" class="table table-striped table-bordered">
                <thead class="table-dark">
                    <tr>
                        <th>Chi Nhánh</th>
                        <th>Mã CTKM</th>
                        <th>Tên CTKM</th>
                        <th>Doanh Thu(VND)</th>
                        <th>CPKM(VND)</th>
                        <th>%KM</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (DataRow row2 in Model.Tables[0].Rows)
                    {
                        var maDvcs = row2["Ma_Dvcs"].ToString(); // Lấy giá trị Ma_Dvcs từ vòng lặp

                        <tr>
                            <td hidden name="RowId">@row2["RowId"]</td>
                            <td>
                                <select class="form-select" name="Dvcs" id="Dvcs">
                                    <option value="OPC_B1" @(maDvcs == "OPC_B1" ? "selected" : "")>Công Ty Cổ Phần Dược Phẩm OPC</option>
                                    <option value="OPC_TP" @(maDvcs == "OPC_TP" ? "selected" : "")>Chi Nhánh Hồ Chí Minh</option>
                                    <option value="OPC_CT" @(maDvcs == "OPC_CT" ? "selected" : "")>Chi nhánh Cần Thơ</option>
                                    <option value="OPC_TG" @(maDvcs == "OPC_TG" ? "selected" : "")>Chi Nhánh Tiền Giang</option>
                                    <option value="OPC_MD" @(maDvcs == "OPC_MD" ? "selected" : "")>Chi nhánh Miền Đông</option>
                                    <option value="OPC_VT" @(maDvcs == "OPC_VT" ? "selected" : "")>Chi Nhánh Vũng Tàu</option>
                                    <option value="OPC_NT" @(maDvcs == "OPC_NT" ? "selected" : "")>Chi Nhánh Nha Trang</option>
                                    <option value="OPC_DN" @(maDvcs == "OPC_DN" ? "selected" : "")>Chi Nhánh Đà Nẵng</option>
                                    <option value="OPC_NA" @(maDvcs == "OPC_NA" ? "selected" : "")>Chi Nhánh Nghệ An</option>
                                    <option value="OPC_HN" @(maDvcs == "OPC_HN" ? "selected" : "")>Chi Nhánh Hà Nội</option>
                                </select>
                            </td>
                            <td><input name="Ma_CTKM" id="Ma_CTKM" class="ma-ctkm-input" style="width:150px" value="@row2["Ma_CTKM"]"></td>
                            <td><input type="text" name="Ten_CTKM" class="ten-ctkm-input" style="width:200px" value="@row2["Ten_CTKM"]" /></td>
                            <td><input type="text" name="Doanh_Thu" class="doanh-thu-input" style="width: 150px; text-align: right" value="@row2["Doanh_Thu"]" /></td>
                            <td><input type="text" name="CPKM" class="cpkm-input" style="width: 150px; text-align: right" value="@row2["CTKM"]" /></td>
                            <td><input type="text" name="Ty_Le" class="ty-le-input" style="width: 150px; text-align: right" value="@row2["Ty_Le"]" readonly /></td>
                            <td><button class="deleteRowBtn">Xóa</button></td>
                        </tr>
                    }
                </tbody>

            </table>
                    <button type="button" id="addRowBtn">Thêm dòng</button>
                    <button class="submit" type="submit" id="SaveCtV">Lưu</button>
                </form>

        </div>
    </section>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="/assets/bootstrap/js/bootstrap.min.js"></script>
    <script src="/assets/js/Application-Form-1.js"></script>
    <script src="/assets/js/Application-Form.js"></script>
    @*</ng-><script src="/assets/js/script.js"></script>*@
    <script>
        function setCurrentDate() {
            const currentDate = new Date();
            const formattedToDate = formatDate(currentDate);
            document.getElementById("Ngay_Ct").value = formattedToDate;
        }
        function formatDate(date) {
            const year = date.getFullYear();
            const month = (date.getMonth() + 1).toString().padStart(2, "0");
            const day = date.getDate().toString().padStart(2, "0");
            return `${year}-${month}-${day}`;
        }
        setCurrentDate();
    </script>
    <script>
        $(document).ready(function () {
            // Function to calculate Ty_Le
            function calculateTyLe() {
                // Iterate through each row in the table
                $("#example tbody tr").each(function () {
                    var doanhThu = parseFloat($(this).find('input[name="Doanh_Thu"]').val()) || 0;
                    var cpkm = parseFloat($(this).find('input[name="CPKM"]').val()) || 0;
                    var tyLeInput = $(this).find('input[name="Ty_Le"]');

                    // Calculate Ty Le if CPKM is greater than 0
                    if (cpkm > 0) {
                        var tyLe = doanhThu / cpkm;
                        tyLeInput.val(tyLe.toFixed(2)); // Round to 2 decimal places
                    } else {
                        tyLeInput.val(0); // Set Ty Le to 0 if CPKM is 0 or less
                    }
                });
            }

            // Event listeners for Doanh_Thu and CPKM inputs
            $(document).on('input', 'input[name="Doanh_Thu"], input[name="CPKM"]', function () {
                calculateTyLe();
            });
        });

    </script>
   

    <script>
        $(document).ready(function () {
            // Function to repopulate dropdownListVT


            $('#addRowBtn').click(function () {
                var newRow = '<tr>' +
                   
                    '<td><select class="form-select" name="Dvcs" id="Dvcs">' +
                    '<option value="OPC_B1" selected="">Công Ty Cổ Phần Dược Phẩm OPC</option>' +
                    '<option value="OPC_TP" selected="">Chi Nhánh Hồ Chí Minh</option>' +
                    '<option value="OPC_CT" selected="">Chi nhánh Cần Thơ</option>' +
                    '<option value="OPC_TG" selected="">Chi Nhánh Tiền Giang</option>' +
                    '<option value="OPC_MD" selected="">Chi nhánh Miền Đông</option>' +
                    '<option value="OPC_VT" selected="">Chi Nhánh Vũng Tàu</option>' +
                    '<option value="OPC_NT" selected="">Chi Nhánh Nha Trang</option>' +
                    '<option value="OPC_DN" selected="">Chi Nhánh Đà Nẵng</option>' +
                    '<option value="OPC_NA" selected="">Chi Nhánh Nghệ An</option>' +
                    '<option value="OPC_HN" selected="">Chi Nhánh Hà Nội</option></select>' +
                    '</td>' +
                    '<td><input name="Ma_CTKM" id="Ma_CTKM" class="ma-ctkm-input" style="width:150px" ></td>' +
                    ' <td> <input type="text" name="Ten_CTKM" class="ten-ctkm-input" style="width:200px"/></td>' +
                    '<td> <input type="text" name="Doanh_Thu" class="doanh-thu-input" style="width:150px;text-align:right" /></td>' +
                    '<td> <input type="text" name="CPKM" class="cpkm-input" style="width:150px;text-align:right" /></td>' +
                    '<td> <input type="text" name="Ty_Le" class="ty-le-input" style="width:150px;text-align:right" readonly/></td>' +
                    '<td><button class="deleteRowBtn">Xóa</button></td>' +
                    '</tr>';
                $('#example tbody').append(newRow);

                // Call the function to recreate the dropdownListVT

            });

            //$(document).on('input', '.ma-vt-input', function () {
            //    var inputValue = $(this).val();
            //    var tenVtInput = $(this).closest('tr').find('.ten-vt-input');

            //    // Find the corresponding option in dropdownListVT
            //    var option = $('#dropdownListVT option[value="' + inputValue + '"]');

            //    if (option.length > 0) {
            //        // Update the Ten_vt input with the data-ten attribute of the selected option
            //        tenVtInput.val(option.attr('data-ten'));
            //    } else {
            //        tenVtInput.val('');
            //    }
            //});

            $(document).on('click', '.deleteRowBtn', function () {
                $(this).closest('tr').remove();

                // Call the function to recreate the dropdownListVT after deleting a row
                /*            populateDropdownListVT();*/
            });
        });

        function SaveCtV(data) {
            return $.ajax({
                contentType: 'application/json; charset=utf-8',
                dataType: 'json',
                type: 'POST',
                url: "/FormDTChiPhiKM/Update",
                data: JSON.stringify(data), // Ensure data is stringified
                success: function (result) {
                    alert(result);
                    window.location.href = '/FormDTChiPhiKM/Index';
                },
                error: function (xhr, status, error) {
                    console.log("Error details:", xhr.responseText); // Log nội dung response từ server
                    var errorMessage = xhr.status + ': ' + xhr.statusText;
                    alert('Error - ' + errorMessage);
                }
            });
        }




        $("#SaveCtV").click(function (e) {
            e.preventDefault();

            var orderArr = [];

            var ngayCt = $("#Ngay_Ct").val();
            var soCt = $("#So_Ct").val();

            $("#example tbody tr").each(function () {
                var dvcs = $(this).find('select[name="Dvcs"]').val();
                var maCtkm = $(this).find('input[name="Ma_CTKM"]').val();
                var tenCtkm = $(this).find('input[name="Ten_CTKM"]').val();
                var doanhThu = $(this).find('input[name="Doanh_Thu"]').val();
                var cpkm = $(this).find('input[name="CPKM"]').val();
                var tyLe = parseFloat($(this).find('input[name="Ty_Le"]').val()) || 0;
               

                orderArr.push({
                   
                    Ngay_Ct: ngayCt,
                    So_Ct: soCt,
                    Ma_Dvcs: dvcs,
                    Ma_CTKM: maCtkm,
                    Ten_CTKM: tenCtkm,
                    Doanh_Thu: doanhThu,
                    CPKM: cpkm,
                    Ty_Le: tyLe
                });
            });

            // Prepare and send the data
            var data = orderArr;
            console.log("Data", data);
            $.when(SaveCtV(data)).then(function (response) {
                console.log(response);
            }).fail(function (err) {
                console.log(err);
            });
        });


    </script>






</body>


</html>
